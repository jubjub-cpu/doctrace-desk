# The v1.0.0 recruiter demo is deterministic and requires no secrets.
DOCUTRACE_MODE=deterministic

# Reserved for a future optional bring-your-own-key adapter. Keep real values in .env only.
OPENAI_API_KEY=
