# Validation Evidence

## Release Status

Release candidate validation completed on 2026-07-17. The local and deployed workflows passed the same browser suite.

## Logic Tests

Result:

```text
DOCUTRACE LOGIC TESTS PASSED
Checked retrieval, empty results, extraction, version comparison, risk flags, and review-summary evidence.
```

The test module uses the same `assets/analysis.mjs` functions shipped to the browser.

## Repository Validation

Result:

```text
DOCUTRACE VALIDATION PASSED
Checked repository structure, synthetic fixtures, disclosures, privacy patterns, accessible HTML hooks, and analysis logic.
```

The validation script checks required release files, valid JSON, two packets, three documents per packet, synthetic-data notices, disclosure language, private-information patterns, common secret patterns, accessible HTML hooks, and logic-test results.

## Browser Workflow

Result:

```text
DOCUTRACE BROWSER TESTS PASSED
```

Verified:

- Desktop viewport: 1440px wide
- Mobile viewport: 390px wide
- Northstar packet: approved after citation and field verification
- Harborline packet: returned for clarification
- Keyboard: skip link is the first focus stop and targets the review workspace
- Download: local review text filename verified
- Mobile search input: 47px high
- Horizontal overflow: none on desktop or mobile
- States: loading, validation, no evidence, load error, pending, approved, returned
- Console errors: 0
- Failed requests in the normal workflow: 0

## Screenshots

- `docs/screenshots/doctrace-approved-workflow.png`: 1440px desktop evidence workspace
- `docs/screenshots/doctrace-mobile-workflow.png`: 390px mobile evidence workspace

Both screenshots were regenerated by the passing browser suite after the responsive input fix.

## Privacy and Security

- Fixture content is explicitly synthetic and fictional.
- No personal email address, customer record, private document, credential, or production log is included.
- Query text is escaped before rendering.
- No app data leaves the browser.
- The release requires no API key.
- The repository validation scans for common API-token and private-key patterns.

## Deployment

- GitHub Pages status: `built`
- Live URL: https://jubjub-cpu.github.io/doctrace-desk/
- Main page response: HTTP 200
- Synthetic packet response: HTTP 200
- Deployed title: `DocuTrace Desk | Evidence Review Workspace`
- Deployed browser suite: passed
- Deployed desktop approval: passed
- Deployed mobile approval: passed
- Deployed second-packet return decision: passed
- Deployed keyboard skip link: passed
- Deployed console errors: 0
- Deployed failed requests in the normal workflow: 0

The optional GitHub Actions workflow remains omitted because the current publishing token does not include workflow scope. This does not weaken the checked-in logic, repository, local-browser, or deployed-browser evidence.
